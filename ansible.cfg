[defaults]
# Use the Beautiful Output callback plugin.
stdout_callback = beautiful_output
# Use the stdout_callback when running ad-hoc commands.
bin_ansible_callbacks = True
verbose_to_stderr = True
# Opt into the future (2.24+) behavior: facts only via ansible_facts, not
# injected as top-level ansible_* variables.
inject_facts_as_vars = False
